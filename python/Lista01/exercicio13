razao = float(input("Digite a razão da PG: "))
primeiro_termo = float(input("Digite o valor do primeiro termo: "))
decimo_termo = primeiro_termo * (razao ** 9)

print(f"décimo termo: {decimo_termo:.1f}")
